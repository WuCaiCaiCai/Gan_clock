# 苷 开发日志

## 项目概述

苷是一个偏安静、偏手机使用场景的番茄钟。主流程使用 Flutter + Dart + Material Design 3 实现，重点放在沉浸计时、清晰统计、Android 画中画和可备份的数据记录上。

**当前阶段:** GitHub 开源文档整理，README 首屏重构，开发日志结构清理。

## 当前状态

- 应用名称: 苷
- 版本号: `0.1.0+1`
- 主要平台: Android
- 发布状态: 暂不发版，先完成开源仓库准备
- 语言边界: 番茄钟业务逻辑全 Dart；Android Java 只作为 PiP 和屏幕常亮的系统桥接
- Kotlin 状态: 无 Kotlin 源码，无 Kotlin Gradle DSL

## 已完成能力

### 计时核心

- [x] `TomatoTimerEngine` 负责启动、暂停、停止、休息跳过和阶段自动衔接。
- [x] 专注完成后自动进入短休息或长休息，休息结束后自动回到专注。
- [x] 停止正在进行的专注时，满 60 秒会写入未完成记录，不满 60 秒不记录。
- [x] 停止后统一回到新一轮专注，不停留在休息或旧进度。
- [x] 跳过只允许跳过运行中的休息，不允许跳过正在进行的番茄钟。

### 数据与统计

- [x] `TimerMode`, `TimerPhase`, `AppSettings`, `WebDavSettings`, `FocusSession`, `TimerSnapshot`, `TomatoData` 数据模型已完成。
- [x] JSON 序列化和反序列化已完成。
- [x] 本地存储使用 tmp 文件 + rename 的原子写入策略。
- [x] 今日专注、总专注时长、最近记录和月/年热力图已完成。
- [x] 热力图支持点击日期查看当天专注时间，宽屏和窄屏布局已重新分配。

### 备份同步

- [x] WebDAV 上传、下载、同步已完成。
- [x] 支持 Basic Auth，远端目录不存在时会逐级创建。
- [x] 本地数据和远端数据按规则合并，专注记录按 `id` 去重。
- [x] 备份入口收进设置页，包含 WebDAV、本地备份、自动同步开关和同步间隔。
- [x] 本地备份支持指定目录，未指定时写入应用数据目录下的 `local_backups/`。
- [x] 应用存活期自动同步已完成，前台、未挂起后台或 PiP 状态下会按间隔尝试同步。

### 界面与交互

- [x] 一级界面拆为番茄钟、统计、设置。
- [x] 设置页采用条目式入口，不在一级列表暴露过多二级配置。
- [x] 主计时页移除顶部大标题，改为全屏沉浸式布局和悬浮 dock。
- [x] 阶段色同步作用于背景、计时圆环和 PiP 小窗。
- [x] 专注为红褐色，短休息为绿色，长休息为沉浸蓝色。
- [x] 运行中无操作或点击空白区域后隐藏其他元素，仅保留一言和计时圆环。
- [x] 一言接入官方接口，失败时使用本地兜底短句，字体使用霞鹜文楷。

### Android 能力

- [x] Android 入口为 Java `MainActivity`，保留 FlutterActivity 嵌入。
- [x] Android 画中画接入完成，运行中回到后台或点击按钮可进入 PiP。
- [x] 屏幕常亮通过 Android 窗口标记桥接。
- [x] Android Gradle 脚本使用 Groovy，已移除 Kotlin 插件和 KTS 文件。
- [x] Android 启动图标由根目录 `icon.svg` 生成自适应 foreground 和各密度 PNG。

### 工程与开源

- [x] `flutter analyze` 通过。
- [x] Debug APK 构建通过。
- [x] `.gitignore` 已核验，排除构建产物、签名文件和本地密钥。
- [x] MIT License 已加入。
- [x] README 已重构为 GitHub 展示友好的版本，并补充技术栈标签。

## 暂缓事项

- [ ] iOS 后台计时与通知适配。
- [ ] 桌面端迷你窗口方案。
- [ ] 各平台原生通知细化。
- [ ] Android 应用被系统完全杀死后的周期联网同步。当前实现是应用存活期定时同步；杀死后继续定时联网需要 WorkManager 这类原生后台任务。

## 关键逻辑说明

### 计时记录

- 60 秒以下的专注不写入记录，也不参与今日统计、总时长和热力图。
- 完整完成的专注记录会标记 `completed=true`，停止时满 60 秒的记录会标记 `completed=false`。
- 长休息由已完成专注轮数决定，默认每 4 轮进入一次长休息。

### WebDAV 合并

- 远端文件不存在时会创建新备份。
- 远端目录不存在时会逐级创建。
- 专注记录按 `id` 去重并按结束时间排序。
- 设置和活动计时器采用更新时间较新的版本。

### 平台边界

- Dart 层负责计时、记录、统计、设置、同步和 UI。
- Java 层只负责 Android 系统 API 桥接，包括 PiP 参数、PiP 状态回调和屏幕常亮。
- 项目没有 Kotlin，也没有 Kotlin Gradle DSL。

## 开发日志

### 2026-05-06 开源文档与日志重构

- 重写 README 首屏结构，加入应用图标、项目简介、功能表、技术栈标签、运行命令和目录说明。
- 明确“业务逻辑全 Dart，Android Java 仅作系统桥接”的技术边界，避免把 PiP/屏幕常亮桥接误解为业务代码。
- 清理开发日志顶部结构，将已完成能力、暂缓事项和关键逻辑说明拆开，减少“待开发”里混入已完成事项的问题。
- 保留历史开发日志，便于继续追踪计时、统计、备份、PiP、图标和开源准备的每次变更。

### 2026-05-06 再次替换 SVG 启动图标
- 接收新的根目录 `icon.svg` 作为图标源文件。
- 重新生成 Android 自适应图标 foreground，保留亮色和夜间资源一致。
- 重新导出 `mipmap-mdpi` 到 `mipmap-xxxhdpi` 的 legacy launcher PNG。

### 2026-05-06 统计布局与本地备份目录
- 统计页删除“今日番茄”计数小卡片，只保留今日专注时长和总专注时长。
- 热力图重新分配摘要、图块、选中日期和图例的位置，宽屏侧栏展示摘要，窄屏先摘要后图块。
- 设置页入口 subtitle 改成短说明，不再把二级菜单里的具体配置值直接铺在一级列表上。
- 本地备份支持指定目录，留空时继续写入应用数据目录下的 `local_backups`。
- 本地备份成功/失败改为备份页卡片内状态，不再通过底部 SnackBar 提示，避免被底部弹层遮挡。

### 2026-05-06 备份入口与 PiP 圆环
- 底部 dock 精简为“番茄钟 / 统计 / 设置”，备份入口收进设置条目，减少一级菜单负担。
- 备份页新增“本地备份”，会在应用数据目录写入 `local_backups/gan_backup_YYYYMMDD_HHMMSS.json`。
- 选项卡切换动画缩短并去掉横向滑动，dock 选中态动画同步加快，降低点击后的迟滞感。
- 画中画从纯数字方块改回阶段色圆环，Java 侧 `sourceRectHint` 收敛到圆环区域；返回全屏时增加圆环放大展开动画，减轻小窗回主界面的切割感。

### 2026-05-06 接入一言接口
- 新增 `hitokoto_service.dart`，使用官方 `v1.hitokoto.cn` JSON 接口获取短句，并设置短超时、状态码校验和 JSON 解析保护。
- 一言组件启动时先显示本地阶段短句，网络成功后淡入替换；请求结果按应用进程缓存，避免频繁访问接口。
- 网络不可用、接口异常或返回内容过长时保持本地兜底，不影响番茄钟主界面和测试稳定性。

### 2026-05-06 应用改名与数字输入
- 应用展示名改为“苷”，同步更新 Flutter 标题、Android/iOS/macOS/Linux/Windows 展示名称和开源文档。
- `NumberStepper` 改为支持手动输入整数，失焦或提交后自动限制在设置范围内。
- 专注、短休息、长休息和备份同步间隔的左右按钮步进改为 5 分钟；长休间隔轮数和静默秒数保持原有 1 步进逻辑。

### 2026-05-06 备份、PiP 过渡与开源准备
- WebDAV 从设置页移入独立“备份”页，集中管理远端配置、手动同步、自动同步开关和同步间隔。
- `AppController` 增加应用存活期自动同步：配置 WebDAV 后按间隔同步，进入后台前也会尝试静默同步一次。
- 停止计时后统一回到新一轮专注，不再停留在短休息或长休息的上次进度。
- PiP 进入前先切换到方形数字小窗，减少后台切换时闪出缩小圆环；退出 PiP 后延迟恢复 dock，减轻全屏返回的切割感。
- 计时圆环整体缩小 10%，dock 点击动画缩短并去掉滞留水波反馈。
- 核验并补充 `.gitignore`，重写 GitHub README，新增 MIT License，明确 Dart 业务逻辑和 Android Java 系统桥接边界。

### 2026-05-06 画中画方形重构
- PiP 布局从宽胶囊改为近方形遮罩，只保留阶段色背景和自动反色数字倒计时。
- 移除 PiP 内嵌常亮按钮和原生 RemoteAction，小窗内容保持纯数字时钟。
- Android PiP 比例从 `21:9` 改为 `1:1`，并使用居中方形 `sourceRectHint` 优化收缩动画。
- 后台进入 PiP 前先切换到计时页静默状态，Android 12 及以上交给系统自动进入，减少重复触发造成的不跟手。

### 2026-05-06 重新替换 SVG 启动图标
- 使用更新后的 `icon.svg` 重新生成 Android 自适应图标 foreground，去掉上一个版本的偏移适配。
- 同步重新导出 `mipmap-mdpi` 到 `mipmap-xxxhdpi` 的 legacy launcher PNG，保证新图标在不同启动器下保持一致。

### 2026-05-06 使用用户提供 SVG 图标
- 将根目录 `icon.svg` 作为启动图标源文件纳入项目。
- Android 自适应图标 foreground 改为由该 SVG 生成的 VectorDrawable，亮色和夜间资源保持一致。
- 重新生成 `mipmap-mdpi` 到 `mipmap-xxxhdpi` 的 legacy launcher PNG，兼容不使用 adaptive icon 的启动器。

### 2026-05-06 视觉重心与 PiP 进度
- 计时圆环和一言整体上移，让主视觉从屏幕正中略微抬高，更适合手机握持视线。
- 沉浸模式改为隐藏 Android 系统状态/导航栏，并保留一言与计时圆环。
- dock 选中遮罩加高，改成轻量描边胶囊，保留现有大 dock 比例但减少扁平突兀感。
- PiP 胶囊重做为方向性渐变进度：专注从左到右由浅入深，休息从右到左由深到浅，减少内边距和未使用空间。

### 2026-05-06 连续计时与手机端比例
- 修正阶段完成后的推进逻辑：专注完成后直接运行休息，休息完成后直接运行下一轮专注，不再停到 idle。
- 底部 dock 改为自绘胶囊导航，去掉 NavigationBar 外层突兀的形状和多余高度。
- 开始/停止操作栏改为单行紧凑胶囊，固定在 dock 上方，窄屏下停止按钮自动收为图标，避免覆盖 dock。
- 一言改为阶段色短条样式，使用同阶段色细线和引用图标，与计时圆环形成视觉呼应。

### 2026-05-06 一言位置与字体
- 一言从计时圆环下方移到圆环上方，避免和下方操作区竞争视觉重心。
- 注册 `LXGWWenKaiMono-Regular.ttf` 字体资源，作为 `LXGW WenKai` 在 Flutter 中使用，并保留霞鹜文楷相关字体名 fallback。

### 2026-05-06 圆环计时与 PiP 胶囊
- 正常打开应用时恢复阶段色计时圆环，专注、短休息、长休息分别使用红褐、绿色和沉浸蓝。
- PiP 进入/退出通过 Java `MainActivity` 回传给 Flutter，PiP 内切换为居中的胶囊小窗，使用阶段色背景和自动反色数字显示剩余时间。
- Android PiP 参数改为宽胶囊比例，并加入“屏幕常亮”原生动作；切换结果会同步回 Dart 设置。
- 跳过逻辑改为仅允许运行中的短休息/长休息跳回运行中的专注，不允许跳过正在进行的番茄钟，也不会把计时器停到 idle。
- 移除正常计时页底部背景进度条，系统导航栏颜色同步阶段背景，避免沉浸界面底部残留异色条。

### 2026-05-06 数字计时与画中画配色
- 计时器改为纯数字倒计时，减少小屏和 PiP 下的布局挤压。
- 阶段配色重调：全局主题固定书架棕基调；专注为红褐，短休息为绿色，长休息为沉浸蓝。
- 计时页背景、数字、阶段标记和进度层同步使用阶段色，PiP 静默布局里也能看到背景进度。
- 进入后台时主动切到静默数字布局并请求 Android PiP，减少画中画里控件挤在一起的问题。
- 启动图标改为地球 emoji 风格自绘矢量图标，保留亮色/夜间资源。

### 2026-05-06 沉浸计时与 Android 画中画
- 修复停止后专注时间不统计的问题：停止专注时满 60 秒会写入 `FocusSession`，计入专注时长、热力图和最近记录，但 `completed=false`，不算完成番茄。
- 主计时页移除顶部 AppBar 和手动阶段切换，使用全屏阶段背景色提示专注、短休息、长休息。
- 计时器固定在屏幕中心，静默显示只对一言、状态胶囊、操作区和悬浮 dock 做淡出/滑出动画，计时器位置不再跳变。
- 支持点击计时页空白区域立即进入静默显示，触摸计时器区域恢复控件。
- 操作区增加屏幕常亮和画中画按钮，开始和停止加入触感反馈，并配合计时器启动动效。
- Android 增加 Java `MainActivity`，通过轻量 MethodChannel 控制屏幕常亮、PiP 状态和后台自动进入画中画；不引入 Kotlin。
- 新增亮色/夜间自适应矢量启动图标。

### 2026-05-06 主题跟随系统与热力图选日
- 外观设置从布尔夜间模式升级为 `AppThemeMode`，支持“跟随系统 / 浅色 / 夜间”三档，并兼容旧的 `darkModeEnabled` 本地数据。
- `MaterialApp.themeMode` 改为根据外观设置映射到 `ThemeMode.system/light/dark`。
- 热力图日期块增加点击选中状态，月视图和年视图都可以切换选中日期，下方显示该天专注时间。
- 补充 Widget 测试，覆盖三档主题切换和点击热力图日期后的当天专注时间展示。

### 2026-05-06 夜间模式与记录阈值
- 新增夜间主题和“外观”设置条目，支持手动切换深色界面。
- Material 主题拆分为亮色/深色两套，计时文字、统计卡片和热力图颜色改为跟随 `ColorScheme`。
- `FocusSession` 增加 60 秒有效记录阈值，少于 1 分钟的专注结束后不写入记录，也不计入今日统计、总时长、热力图或最近专注。
- 补充单元测试和 Widget 测试，覆盖夜间模式序列化/切换，以及短于 1 分钟记录的过滤。

### 2026-05-06 番茄钟静默模式
- 主计时页增加停止键，停止当前运行或暂停的计时后回到当前阶段的初始时长，不写入完成记录。
- 番茄钟下方增加本地“一言”，按专注、短休息、长休息显示不同短句，不依赖网络请求。
- 新增 `idleFocusSeconds` 设置，运行中超过设定秒数无操作后隐藏顶部栏、底部导航和操作按钮，仅保留番茄钟；点击或触摸后恢复完整界面。
- 补充计时引擎与 Widget 测试，覆盖停止键、静默显示和设置序列化。

### 2026-05-06 启动动效优化
- 计时器改为局部 Stateful 动画，启动时只对核心计时显示做轻微缩放和脉冲。
- 进度变化从线性跳变改为 `easeOutCubic` 补间，减少每秒 tick 的生硬感。
- 模式图标和阶段文字使用短时 `AnimatedSwitcher` 淡切，避免整页一起动。

### 2026-05-06 信息架构调整
- 主界面聚焦番茄钟本身，只保留倒计时和操作按钮。
- 统计信息移入独立“统计”页，包含今日统计、热力图和最近专注记录。
- 设置改为条目式入口，“计时设置 / 切换提醒 / WebDAV 同步”分别打开独立配置面板。

### 2026-05-06 震动提醒
- 阶段切换提醒改为手机震动优先：默认关闭提示音、开启震动。
- `completion_feedback.dart` 从 `HapticFeedback.mediumImpact()` 改为 `HapticFeedback.vibrate()`，更接近手机震动提醒。
- 设置页文案改为“切换震动 / 切换音效”，明确用于专注与休息阶段切换。

### 2026-05-06 纯 Dart 收尾
- 移除 Android Kotlin 源码：删除 `MainActivity.kt`、`TomatoOverlayService.kt`，Manifest 直接使用 Flutter `FlutterActivity`。
- 移除 Kotlin DSL：`settings.gradle.kts`、`build.gradle.kts`、`app/build.gradle.kts` 改为 Groovy Gradle 文件，删除 Kotlin Android 插件。
- 移除 Dart `MethodChannel` 桥接与悬浮窗开关，项目主业务代码保持纯 Dart。
- 新增 `completion_feedback.dart`，计时完成后根据设置播放系统提示音并触发触感反馈。
- 新增 `heatmap.dart`，支持月/年范围切换的专注热力图。
- 补充控制器测试，覆盖应用启动时对过期计时器的 Dart 侧结算与完成反馈触发。

### 2026-05-06 主计时闭环
- 新增纯 Dart `TomatoTimerEngine`，负责计时启动、暂停、重置、跳过、完成后自动进入下一阶段，并在专注完成时写入 `FocusSession`。
- 新增 `AppController`，用 `ChangeNotifier` 串联本地存储、WebDAV 和计时器 tick。
- 新增 Material 3 主界面，包含倒计时、开始/暂停/重置/跳过、今日统计、最近记录和设置面板。
- 设置面板支持自定义专注/休息时长、长休间隔、WebDAV 配置保存和手动同步。
- 补充 Widget smoke test 与计时引擎单元测试。
